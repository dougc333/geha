import { useState } from 'react'
import type { Claim, Decision, Review } from '../../shared/types'
export function ReviewPanel({claim,review,busy,onDecision}:{claim:Claim;review:Review|null;busy:boolean;onDecision:(d:Decision)=>Promise<void>}){
 const [text,setText]=useState(review?.draft??''),[reason,setReason]=useState('')
 return <div className="rounded-2xl border border-slate-200 bg-white shadow-sm overflow-hidden"><div className="border-b border-slate-200 px-6 py-5"><h2 className="font-semibold text-lg">{review?'Review the explanation':'Recorded claim context'}</h2><p className="text-sm text-slate-500 mt-1">{review?'Your decision releases text—not an insurance claim.':'Start a review to draft an explanation and pause for human approval.'}</p></div><div className="p-6">
 {!review?<><p className="leading-relaxed">{claim.reason}</p><div className="mt-7 rounded-xl bg-slate-50 p-5 text-sm text-slate-600">Two steps: generate a template explanation, then pause at a LangGraph interrupt for your review.</div></>:review.status==='waiting'?<form onSubmit={e=>e.preventDefault()}><label className="block text-sm font-semibold mb-2" htmlFor="explanation">Explanation to release</label><textarea id="explanation" value={text} onChange={e=>setText(e.target.value)} maxLength={8000} rows={10} className="input resize-y leading-relaxed"/><label className="block text-sm font-semibold mt-5 mb-2" htmlFor="reason">Review reason <span className="font-normal text-slate-500">(required)</span></label><input id="reason" value={reason} onChange={e=>setReason(e.target.value)} maxLength={1000} placeholder="What did you verify or what needs correcting?" className="input"/><div className="flex flex-wrap gap-3 mt-5"><button className="primary" disabled={busy||!reason.trim()||!text.trim()} onClick={()=>void onDecision({action:'approve',reason,text})}>Approve explanation</button><button className="rounded-lg border border-red-200 text-red-700 px-4 py-2.5 font-medium" disabled={busy||!reason.trim()||!text.trim()} onClick={()=>void onDecision({action:'reject',reason,text})}>Reject explanation</button></div></form>:<><div className={`rounded-lg p-3 mb-5 text-sm font-medium ${review.status==='approved'?'bg-teal-50 text-teal-900':'bg-red-50 text-red-800'}`}>{review.status==='approved'?'Explanation approved':'Explanation rejected'} · Claim status unchanged</div><p className="whitespace-pre-wrap leading-relaxed">{review.answer}</p><p className="mt-6 text-sm text-slate-500">Reviewer reason: {review.decision?.reason}</p></>}
 </div></div>
}
