[project]
name = "geha-naive-chroma"
version = "0.1.0"
description = "Plain-chunk Chroma search baseline for GEHA coverage-policy PDFs"
requires-python = ">=3.11,<3.14"
dependencies = [
    "chromadb>=1,<2",
    "pdfplumber>=0.11,<1",
    "streamlit>=1.40,<2",
]
