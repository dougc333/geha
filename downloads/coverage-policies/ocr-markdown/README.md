There are errors here. 

We used the image screenshot followed by a multimodal parsing using chatGPT to align the table image with the text. Add vertical lines with image columns. 

Those earlier OCR files contain | separators, but some table relationships are incorrect. Bendamustine, for example, merges the Treanda and Dr Reddy’s rows and combines codes J903 J9999.
