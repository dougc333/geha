# Superseded run

This run grouped all split pages back into three original-policy inputs. It is
superseded because each `*-page-NNN.pdf` must be processed as its own LangGraph
input.

Use the corrected run:

`../run-20260925T192637Z-ab798565/`
