[project]
name = "geha-simulation-mcp"
version = "0.1.0"
description = "Local MCP interface for fabricated simulations and read-only policy RAG"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.20,<2",
    "openai>=3.13.0",
    "pgvector>=0.5.0",
    "psycopg[binary]>=3.3.5",
    "python-dotenv>=1.2.3",
    "sentence-transformers>=6.0.1",
]

[tool.uv]
package = false
