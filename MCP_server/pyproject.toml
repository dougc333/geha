[project]
name = "geha-simulation-mcp"
version = "0.1.0"
description = "Local MCP interface for nine fabricated GEHA simulations"
requires-python = ">=3.11"
dependencies = ["mcp>=1.20,<2"]

[tool.uv]
package = false
