

This works. Use table aware parsing.

The internet says table aware parsing doesn't work because of the errors created when the table is parsed to rows and columns. This is true. 

Perserve the table as a table and take a screenshot. Tell the LLM to match the image with the text in the pdf. The pdf doesn't need OCR. There is text backing the rendered text which can be downloaded by any pdf lib. To make sure the tables are preserved with column and spacing be precise to the LLM to keep the formatting. 


